import 'package:flutter/material.dart';

class State extends StatelessWidget {
  const State({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('状態管理');
  }
}
